# Tools (non-production)

This folder holds one-off scripts used for analysis/diagnostics during development.

- `tools/diagnostics/` — inspection scripts for outputs/logs
- `tools/analysis/` — analysis utilities (coverage, overlap, dedupe, etc.)

